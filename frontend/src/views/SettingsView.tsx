const SettingsView = () => {
  return (
    <div className="card">
      <h2 style={{ marginBottom: '1rem', color: 'var(--brand-blue)' }}>Platform Settings</h2>
      <p className="text-secondary" style={{ marginBottom: '2rem' }}>
        Configure general system preferences and API behaviors.
      </p>
      <div className="glass-panel" style={{ padding: '2rem', textAlign: 'center' }}>
        <p className="text-tertiary">Settings placeholder...</p>
      </div>
    </div>
  );
};

export default SettingsView;
